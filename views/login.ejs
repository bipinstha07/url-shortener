<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - URL Shortener</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: #000000;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #ffffff;
            line-height: 1.6;
            position: relative;
        }

        .container {
            width: 100%;
            max-width: 1000px;
            padding: 2rem;
            position: relative;
            z-index: 10;
        }

        .two-column-layout {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 3rem;
            align-items: stretch;
        }

        .left-column {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(20px);
            border-radius: 16px;
            padding: 3rem;
            border: 1px solid rgba(255, 255, 255, 0.1);
            position: relative;
            transition: all 0.3s ease;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            display: flex;
            flex-direction: column;
        }

        .right-column {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(20px);
            border-radius: 16px;
            padding: 3rem;
            border: 1px solid rgba(255, 255, 255, 0.1);
            position: relative;
            transition: all 0.3s ease;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .header {
            text-align: center;
            margin-bottom: 2.5rem;
        }

        .logo {
            font-size: 2.5rem;
            font-weight: 800;
            color: #ffffff;
            margin-bottom: 0.5rem;
            letter-spacing: -0.02em;
        }

        .subtitle {
            color: #a0a0a0;
            font-size: 1.1rem;
            font-weight: 400;
        }

        .form-section {
            animation: slideUp 0.6s ease-out;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 500;
            color: #ffffff;
            font-size: 0.95rem;
        }

        .form-input {
            width: 100%;
            padding: 1rem 1.25rem;
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 8px;
            font-size: 1rem;
            transition: all 0.3s ease;
            background: rgba(255, 255, 255, 0.05);
            color: #ffffff;
            font-family: inherit;
        }

        .form-input:focus {
            outline: none;
            border-color: rgba(255, 255, 255, 0.5);
            background: rgba(255, 255, 255, 0.08);
        }

        .form-input::placeholder {
            color: #666666;
        }

        .submit-btn {
            width: 100%;
            padding: 1rem 2rem;
            background: #ffffff;
            color: #000000;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            font-family: inherit;
            margin-top: 1rem;
        }

        .submit-btn:hover {
            background: rgba(255, 255, 255, 0.9);
        }

        .divider {
            text-align: center;
            margin: 2rem 0;
            position: relative;
            color: #a0a0a0;
            font-size: 0.875rem;
        }

        .divider::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 0;
            right: 0;
            height: 1px;
            background: rgba(255, 255, 255, 0.2);
        }

        .divider span {
            background: rgba(0, 0, 0, 0.8);
            padding: 0 1rem;
            position: relative;
        }

        .signup-link {
            display: block;
            text-align: center;
            padding: 1rem 2rem;
            background: transparent;
            color: #ffffff;
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 8px;
            font-size: 1rem;
            font-weight: 500;
            text-decoration: none;
            transition: all 0.3s ease;
            font-family: inherit;
        }

        .signup-link:hover {
            background: rgba(255, 255, 255, 0.1);
            border-color: rgba(255, 255, 255, 0.5);
        }

        .features {
            margin-top: auto;
            display: grid;
            grid-template-columns: 1fr;
            gap: 1rem;
        }

        .feature {
            text-align: left;
            padding: 1rem;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 12px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .feature-icon {
            font-size: 1.5rem;
            display: block;
            flex-shrink: 0;
            color: #ffffff;
        }

        .feature-content {
            flex: 1;
        }

        .feature-title {
            font-size: 1rem;
            color: #ffffff;
            font-weight: 500;
            margin-bottom: 0.25rem;
        }

        .feature-text {
            font-size: 0.8rem;
            color: #a0a0a0;
            font-weight: 400;
        }

        .welcome-section {
            text-align: center;
            margin-bottom: 1rem;
        }

        .welcome-title {
            font-size: 1.5rem;
            font-weight: 600;
            color: #ffffff;
            margin-bottom: 0.5rem;
        }

        .welcome-description {
            color: #a0a0a0;
            font-size: 1rem;
            line-height: 1.4;
            margin-bottom: 1rem;
        }

        .stats {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 1rem;
            margin-bottom: 1.5rem;
        }

        .stat-item {
            text-align: center;
            padding: 1rem;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 12px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .stat-number {
            font-size: 1.5rem;
            font-weight: 800;
            color: #ffffff;
            margin-bottom: 0.25rem;
        }

        .stat-label {
            font-size: 0.75rem;
            color: #a0a0a0;
            font-weight: 400;
        }

        @media (max-width: 768px) {
            .container {
                padding: 1rem;
            }
            
            .two-column-layout {
                grid-template-columns: 1fr;
                gap: 2rem;
            }
            
            .left-column,
            .right-column {
                padding: 2rem;
            }
            
            .logo {
                font-size: 2rem;
            }
            
            .welcome-title {
                font-size: 1.5rem;
            }
            
            .stats {
                grid-template-columns: 1fr;
            }
        }

        /* Animation classes */
        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Loading animation for button */
        .loading {
            display: inline-block;
            width: 20px;
            height: 20px;
            border: 3px solid rgba(0, 0, 0, 0.3);
            border-radius: 50%;
            border-top-color: #000000;
            animation: spin 1s ease-in-out infinite;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        /* Focus visible for accessibility */
        .submit-btn:focus-visible,
        .form-input:focus-visible,
        .signup-link:focus-visible {
            outline: 2px solid #ffffff;
            outline-offset: 2px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="two-column-layout">
            <!-- Left Column - Login Form -->
            <div class="left-column">
                <div class="header">
                    <h1 class="logo">URL Shortener</h1>
                    <p class="subtitle">Sign in to manage your shortened URLs</p>
                </div>

                <div class="form-section">
                    <form action="/login" method="POST">
                        <div class="form-group">
                            <label for="name" class="form-label">Username</label>
                            <input type="text" id="name" name="name" class="form-input" required 
                                   placeholder="Enter your username">
                        </div>

                        <div class="form-group">
                            <label for="password" class="form-label">Password</label>
                            <input type="password" id="password" name="password" class="form-input" required 
                                   placeholder="Enter your password">
                        </div>

                        <button type="submit" class="submit-btn">
                            Sign In
                        </button>
                    </form>

                    <div class="divider">
                        <span>or</span>
                    </div>

                    <a href="/signup" class="signup-link">
                        Create Account
                    </a>
                </div>
            </div>

            <!-- Right Column - Features & Info -->
            <div class="right-column">
                <div class="welcome-section">
                    <h2 class="welcome-title">Welcome Back!</h2>
                    <p class="welcome-description">
                        Access your URL shortening dashboard and manage all your links in one place. 
                        Track analytics, monitor performance, and create new shortened URLs instantly.
                    </p>
                </div>

                <div class="stats">
                    <div class="stat-item">
                        <div class="stat-number">10K+</div>
                        <div class="stat-label">URLs Shortened</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-number">50K+</div>
                        <div class="stat-label">Total Clicks</div>
                    </div>
                </div>

                <div class="features">
                    <div class="feature">
                        <span class="feature-icon">✂️</span>
                        <div class="feature-content">
                            <div class="feature-title">Shorten URLs</div>
                            <div class="feature-text">Convert long URLs into short, shareable links instantly</div>
                        </div>
                    </div>
                    <div class="feature">
                        <span class="feature-icon">📊</span>
                        <div class="feature-content">
                            <div class="feature-title">Track Analytics</div>
                            <div class="feature-text">Monitor click counts and performance metrics</div>
                        </div>
                    </div>
                    <div class="feature">
                        <span class="feature-icon">🔒</span>
                        <div class="feature-content">
                            <div class="feature-title">Secure & Reliable</div>
                            <div class="feature-text">Your links are secure with 99.9% uptime</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Add loading state to form
        document.querySelector('form').addEventListener('submit', function() {
            const submitBtn = this.querySelector('.submit-btn');
            const originalText = submitBtn.textContent;
            submitBtn.disabled = true;
            submitBtn.innerHTML = '<span class="loading"></span> Signing in...';
            
            // Re-enable after timeout (in case of errors)
            setTimeout(() => {
                submitBtn.disabled = false;
                submitBtn.textContent = originalText;
            }, 10000);
        });

        // Add focus effects
        document.querySelectorAll('.form-input').forEach(input => {
            input.addEventListener('focus', function() {
                this.parentNode.classList.add('focused');
            });
            
            input.addEventListener('blur', function() {
                if (!this.value) {
                    this.parentNode.classList.remove('focused');
                }
            });
        });
    </script>
</body>
</html>
